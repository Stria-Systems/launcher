{
  "name": "stria-launcher",
  "version": "0.1.0",
  "private": true,
  "description": "Stria launcher — installs Stria Works + Stria-Pi, registers the workspace, pairs to the portal.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
